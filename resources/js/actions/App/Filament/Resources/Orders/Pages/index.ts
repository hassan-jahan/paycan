import ListOrders from './ListOrders'
import ViewOrder from './ViewOrder'

const Pages = {
    ListOrders: Object.assign(ListOrders, ListOrders),
    ViewOrder: Object.assign(ViewOrder, ViewOrder),
}

export default Pages